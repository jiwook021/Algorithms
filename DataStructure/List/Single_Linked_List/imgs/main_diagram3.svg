<svg width="1200" height="800" xmlns="http://www.w3.org/2000/svg">
  <rect width="1200" height="800" fill="#f0f0f0"/>
  <text x="50" y="50" font-size="24" fill="black">3. Data Flow / State Transitions</text>
  <rect x="50" y="100" width="1100" height="600" fill="white" stroke="black"/>
  <text x="70" y="150" font-size="20" fill="black">- Initial State: Empty list (head = tail = NULL, sz = 0).</text>
  <text x="70" y="200" font-size="20" fill="black">- After Insert Front: New node becomes head, tail remains same if sz > 1.</text>
  <text x="70" y="250" font-size="20" fill="black">- After Insert Back: New node becomes tail, head remains same if sz > 1.</text>
  <text x="70" y="300" font-size="20" fill="black">- After Reversal: Head and tail swap, node pointers reverse direction.</text>
  <text x="70" y="350" font-size="20" fill="black">- After Free: All nodes and list memory are deallocated.</text>
</svg>