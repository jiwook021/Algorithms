/**
 * @file Stm.hpp
 * @brief STM32 I2C communication example
 * @details Demonstrates I2C master transmit/receive using STM32 HAL drivers.
 *          This is embedded-only code requiring STM32 HAL headers and toolchain.
 *
 * NOTE: This module targets STM32 hardware and cannot be compiled or tested
 *       on a desktop platform. It requires:
 *       - ARM cross-compilation toolchain
 *       - STM32 HAL library headers (main.h, stm32f4xx_hal.h)
 *       - CubeMX-generated initialization code
 */

#pragma once

// This header exists for module structure consistency.
// The actual implementation is in main.cpp which uses STM32 HAL APIs.
